# CLAUDE.md

راهنمای این ریپو برای Claude Code، تا در جلسات بعدی سریع‌تر و بدون خطای تکراری کار کنه.

## این پروژه چیه

یه سایت شخصی/پورتفولیوی تک‌صفحه‌ای (single-page) استاتیک برای مصطفی کشوری، توسعه‌دهنده بک‌اند.
بدون build step، بدون framework، بدون package.json — فقط HTML/CSS/JS خام که مستقیم روی هاست
استاتیک (یا GitHub Pages) سرو می‌شه. زبان صفحه فارسی و RTL (`dir="rtl" lang="fa"`) هست.

## ساختار فایل‌ها

```
index.html           تمام محتوای سایت در یک فایل (~۶۰KB). بخش‌ها به‌صورت
                      div.page با کلاس‌های pt-about / pt-resume / pt-portfolio / pt-contact
                      داخل یه SPA ساده مدیریت می‌شن (نه routing واقعی — همه در یک صفحه‌ن).
css/style-dark.css   استایل اصلی و سفارشی سایت. تغییرات ظاهری معمولاً اینجان.
css/plugins/         کتابخونه‌های CSS شخص ثالث (bootstrap, font-awesome, simplebar,
                      jquery.animatedheadline) — دست نزن مگر برای آپدیت نسخه.
css/settings/        فایل‌های تنظیمات قابل‌تعویض تم (رنگ، نوع باکس، ناوبری، تایتل).
js/main.js           منطق اصلی سایت: پخش موزیک پس‌زمینه، فیلتر portfolio با isotope،
                      منوی موبایل، نمودار مهارت‌ها (easyPieChart)، افکت tilt روی portfolio.
js/glitch-init.js    راه‌انداز افکت glitch روی عنوان اصلی (mgGlitch) — قبلاً inline بود،
                      به فایل جدا منتقل شد تا defer روش کار کنه.
img/portfolio/       تصاویر نمونه‌کارها؛ در بخش isotope مصرف می‌شن.
files/                رزومه PDF قابل دانلود.
robots.txt / sitemap.xml   سئوی فنی پایه؛ چون تک‌صفحه‌ایه، sitemap فقط شامل «/» هست.
```

## نکات حیاتی قبل از هر تغییر

1. **همه‌ی اسکریپت‌ها `defer` دارن و به ترتیب اجرا می‌شن.**
   اگه اسکریپت جدیدی اضافه می‌کنی که به jQuery وابسته‌ست، حتماً `defer` بذار و
   بعد از `js/main.js` قرارش بده — نه به‌صورت inline (چون `defer` روی `<script>`
   بدون `src` اثر نداره و ترتیب اجرا رو به‌هم می‌زنه؛ دلیل وجود `glitch-init.js`
   دقیقاً همینه).

2. **`loading="lazy"` روی تصاویر بخش portfolio ممنوعه.**
   چیدمان اون بخش با `isotope.js` انجام می‌شه که ارتفاع آیتم‌ها رو موقع رویداد
   `window load` بر اساس ابعاد واقعی تصویر محاسبه می‌کنه. اگه تصویر lazy باشه
   و هنوز لود نشده باشه، ارتفاعش صفره → آیتم‌ها روی هم می‌افتن. این باگ قبلاً
   رخ داده، پس دوباره لازیش نکن مگر اینکه هم `imagesLoaded` رو اضافه کنی و
   `isotope.layout()` رو بعد از لود هر عکس صدا بزنی.

3. **سلسله‌مراتب heading رو رعایت کن (h1→h2→h3→h4...، بدون پرش).**
   اگه یه heading باید از نظر بصری کوچیک‌تر/بزرگ‌تر از سایز پیش‌فرض تگش باشه،
   به‌جای عوض کردن سطح heading به یه تگ نامناسب، یه کلاس CSS اختصاصی بساز
   (مثل الگوی `.name-title` که سایز h4 رو روی یه `<h3>` نگه می‌داره).

4. **بدون build step.** تغییرات مستقیم روی فایل‌های HTML/CSS/JS اعمال می‌شن؛
   نیازی به `npm install` یا کامپایل نیست. برای تست محلی کافیه فایل `index.html`
   رو در مرورگر باز کنی یا یه static server ساده بالا بیاری (مثلاً
   `python3 -m http.server`).

5. **سئوی head از قبل کامله** (meta description, OG, Twitter card, JSON-LD برای
   Person و WebSite, canonical, robots). قبل از اضافه کردن متا‌تگ جدید چک کن
   که تکراری نباشه.

## کارهای رایج

- **تغییر محتوای متنی (رزومه، درباره من، نمونه‌کارها):** مستقیم در `index.html`،
  داخل بخش `<div class="page pt-...">` مربوطه.
- **اضافه کردن آیتم جدید به portfolio:** یه `<div class="item col-lg-4 col-sm-6 <category>">`
  جدید داخل `.portfolio-items` اضافه کن؛ `<category>` باید با یکی از فیلترهای
  موجود (`org` / `web` / `shop`) هم‌خوانی داشته باشه یا فیلتر جدید هم به
  `.portfolio-filter ul` اضافه بشه.
- **تغییر رنگ تم:** `css/settings/color/green-color.css` رو ویرایش کن یا فایل
  مشابه جدید بساز و در `<head>` جایگزینش کن.
- **بهینه‌سازی تصاویر:** قبل از اضافه کردن تصویر جدید، حجمش رو چک کن
  (`ls -lh`)؛ `img/home-bg.jpg` با ۳۸۷KB بزرگ‌ترین دارایی سایته و کاندید خوبی
  برای فشرده‌سازی/تبدیل به WebP در آینده‌ست (هنوز انجام نشده).

## گیت و PR

- برنچ اصلی: `master`.
- Commit message‌ها به انگلیسی و مختصر؛ توضیحات جزئی‌تر در بدنه‌ی commit یا PR.
- قبل از باز کردن PR، `git diff --stat` رو چک کن تا مطمئن شی فقط فایل‌های
  مرتبط تغییر کردن (این ریپو فایل‌های باینری حجیم مثل فونت و موزیک هم داره،
  مراقب باش اونا رو دست‌نخورده نگه داری).
